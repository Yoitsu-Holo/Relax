# 集群配置
nodes:
  - id: node-1
    address: localhost:50051
  - id: node-2
    address: localhost:50053
  - id: node-3
    address: localhost:50054

# 健康检查间隔（秒为单位，支持 10s, 1m 等格式）
health_check_interval: 10s

# 集群节点的 gRPC 服务端口
grpc_port: ":50052"

# 集群节点的 HTTP 网关端口
http_port: ":8081"
